<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= course.title %> - 课程详情
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>

<body class="bg-white min-h-screen flex flex-col font-sans">

    <nav class="border-b border-gray-200 sticky top-0 bg-white/95 backdrop-blur z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center">
                <a href="/" class="flex items-center text-gray-800 hover:text-gray-600 transition">
                    <i class="fas fa-arrow-left mr-2"></i>
                    <span class="font-bold text-lg">屯门妇联</span>
                </a>

                <div class="flex items-center space-x-4">
                    <a href="/" class="text-sm text-gray-500 hover:text-gray-900">首页</a>
                    <a href="/contact" class="text-sm text-gray-500 hover:text-gray-900">联系我们</a>
                </div>
            </div>
        </div>
    </nav>

    <main class="flex-grow max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 w-full">

        <nav class="text-sm text-gray-500 mb-8">
            <ol class="list-none p-0 inline-flex">
                <li class="flex items-center">
                    <a href="/" class="hover:text-gray-900">首页</a>
                    <svg class="fill-current w-3 h-3 mx-3" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 320 512">
                        <path
                            d="M285.476 272.971L91.132 467.314c-9.373 9.373-24.569 9.373-33.941 0l-22.667-22.667c-9.357-9.357-9.375-24.522-.04-33.901L188.505 256 34.484 101.255c-9.335-9.379-9.317-24.544.04-33.901l22.667-22.667c9.373-9.373 24.569-9.373 33.941 0L285.475 239.03c9.373 9.372 9.373 24.568.001 33.941z" />
                    </svg>
                </li>
                <li>
                    <span class="text-gray-800 font-medium">
                        <%= course.title %>
                    </span>
                </li>
            </ol>
        </nav>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-10 lg:gap-16">

            <div class="product-image-wrapper">
                <div class="aspect-w-1 aspect-h-1 w-full overflow-hidden rounded-lg bg-gray-100 border border-gray-100">
                    <img src="<%= course.image %>" alt="<%= course.title %>"
                        class="w-full h-full object-contain object-center p-4 hover:scale-105 transition-transform duration-500">
                </div>
            </div>

            <div class="product-info flex flex-col">
                <h1 class="text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl mb-4">
                    <%= course.title %>
                </h1>

                <div class="flex items-end gap-4 mb-6">
                    <p class="text-3xl text-red-600 font-bold">HK$<%= course.price %>
                    </p>
                    <% if(course.originalPrice && course.originalPrice> course.price) { %>
                        <p class="text-lg text-gray-400 line-through mb-1">HK$<%= course.originalPrice %>
                        </p>
                        <span class="mb-2 px-2 py-0.5 rounded bg-red-100 text-red-600 text-xs font-bold">优惠价</span>
                        <% } %>
                </div>

                <div class="border-t border-gray-200 my-4"></div>

                <div class="space-y-4 mb-8">
                    <div>
                        <span class="text-sm font-medium text-gray-500 block mb-2">课程分类</span>
                        <div class="flex flex-wrap gap-2">
                            <% if(course.category && course.category.length> 0) { %>
                                <% course.category.forEach(cat=> { %>
                                    <span
                                        class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-blue-50 text-blue-700 border border-blue-100">
                                        <%= cat %>
                                    </span>
                                    <% }) %>
                                        <% } else { %>
                                            <span class="text-sm text-gray-400">暂无分类</span>
                                            <% } %>
                        </div>
                    </div>

                    <div>
                        <span class="text-sm font-medium text-gray-500 block mb-2">上课时间</span>
                        <div class="flex flex-wrap gap-2">
                            <% if(course.weekDay && course.weekDay.length> 0) { %>
                                <% course.weekDay.forEach(day=> { %>
                                    <span
                                        class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-purple-50 text-purple-700 border border-purple-100">
                                        <i class="far fa-calendar-check mr-1.5"></i>
                                        <%= day %>
                                    </span>
                                    <% }) %>
                                        <% } else { %>
                                            <span class="text-sm text-gray-400">待定</span>
                                            <% } %>
                        </div>
                    </div>

                    <div>
                        <span class="text-sm font-medium text-gray-500 block mb-2">课程周期</span>
                        <div
                            class="flex items-center text-gray-700 bg-gray-50 p-3 rounded border border-gray-100 w-fit">
                            <i class="far fa-clock mr-2 text-gray-400"></i>
                            <span class="font-medium">
                                <%= course.startTime %>
                            </span>
                            <span class="mx-2 text-gray-400">至</span>
                            <span class="font-medium">
                                <%= course.endTime %>
                            </span>
                        </div>
                    </div>
                </div>

                <div class="flex flex-col gap-3">
                    <a href="https://wa.me/85224673867?text=你好，我想查询课程：<%= course.title %>" target="_blank"
                        class="w-full flex items-center justify-center bg-green-600 border border-transparent rounded-md py-4 px-8 text-base font-medium text-white hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 shadow-sm transition">
                        <i class="fab fa-whatsapp mr-2 text-xl"></i> WhatsApp 查询报名
                    </a>

                    <a href="tel:24673867"
                        class="w-full flex items-center justify-center bg-white border border-gray-300 rounded-md py-4 px-8 text-base font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition">
                        <i class="fas fa-phone-alt mr-2 text-gray-400"></i> 致电中心: 2467 3867
                    </a>
                </div>

                <div class="mt-8">
                    <h3 class="text-sm font-medium text-gray-900">课程简介</h3>
                    <div
                        class="mt-4 prose prose-sm text-gray-600 bg-gray-50 p-4 rounded-lg border border-gray-100 leading-relaxed whitespace-pre-wrap"><%= course.description || '暂无详细介绍' %>
                    </div>
                </div>

            </div>
        </div>
    </main>

    <footer class="bg-gray-50 border-t border-gray-200 mt-12">
        <div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8 text-center">
            <p class="text-base text-gray-400">&copy; <%= __('footer_rights') %>
            </p>
        </div>
    </footer>

</body>

</html>