<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>发生错误</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center p-4">

    <div class="bg-white p-8 rounded-2xl shadow-xl max-w-sm w-full text-center border border-gray-100">
        
        <!-- 红色圆圈图标 -->
        <div class="mx-auto flex items-center justify-center h-20 w-20 rounded-full bg-red-100 mb-6">
            <i class="fas fa-times text-red-600 text-4xl"></i>
        </div>
        
        <h2 class="text-2xl font-extrabold text-gray-800 mb-2">操作失败</h2>
        
        <!-- 错误信息 -->
        <p class="text-gray-500 mb-2 font-medium">
            <%= message || '未知错误' %>
        </p>
        
        <% if (error && error.status) { %>
            <p class="text-xs text-red-400 mb-6 bg-red-50 p-2 rounded">
                代码: <%= error.status %> <br>
                <%= error.stack ? '详细信息请查看控制台' : '' %>
            </p>
        <% } else { %>
            <div class="mb-8"></div>
        <% } %>
        
        <button onclick="history.back()" class="block w-full bg-gray-800 hover:bg-gray-900 text-white font-bold py-3 px-4 rounded-xl transition duration-200 shadow-lg transform active:scale-95">
            返回上一页
        </button>
        
        <div class="mt-4">
            <a href="/" class="text-sm text-gray-400 hover:text-gray-600 underline">返回首页</a>
        </div>
    </div>

</body>
</html>