<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>屯門婦聯山景中心 - 課程宣傳</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-50 flex flex-col min-h-screen">

    <nav class="bg-white border-b border-gray-100 sticky top-0 z-50 shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-20 items-center">

                <div class="flex-shrink-0 flex items-center cursor-pointer" onclick="window.location.href='/'">
                    <img class="h-10 w-auto" src="/images/site_logo.webp" alt="Logo" />
                </div>

                <div class="flex-1 max-w-lg mx-4 md:mx-8 relative group">
                    <div class="relative">
                        <input type="text" id="search-input"
                            class="w-full bg-gray-100 border-transparent focus:bg-white border focus:border-blue-500 rounded-md py-2 px-4 pl-10 focus:outline-none transition-colors"
                            placeholder="<%= __('search_placeholder') %>" autocomplete="off" />
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <svg class="h-5 w-5 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                            </svg>
                        </div>
                    </div>

                    <div id="search-results"
                        class="hidden absolute top-full left-0 w-full bg-white shadow-xl rounded-b-md mt-1 z-50 border border-gray-100 max-h-80 overflow-y-auto">
                    </div>
                </div>
                <div class="flex md:hidden ml-2">
                    <button id="mobile-menu-btn" type="button"
                        class="text-gray-500 hover:text-blue-600 focus:outline-none p-2 rounded-md hover:bg-gray-100">
                        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </button>
                </div>
                <div class="hidden md:flex items-center gap-6">

                    <div class="flex space-x-6 text-sm font-medium">
                        <a href="/" class="text-gray-700 hover:text-blue-600 transition">
                            <%= __('nav_home') %>
                        </a>
                        <a href="/about" class="text-gray-700 hover:text-blue-600 transition">
                            <%= __('nav_about') %>
                        </a>
                        <a href="/contact" class="text-gray-700 hover:text-blue-600 transition">
                            <%= __('nav_contact') %>
                        </a>
                    </div>

                    <div class="h-4 w-px bg-gray-300"></div>

                    <div class="flex items-center gap-2 text-xs font-bold text-gray-600">
                        <a href="/lang/zh-CN"
                            class="border border-gray-300 px-2 py-1 hover:bg-blue-50 hover:text-blue-600 hover:border-blue-400 transition <%= getLocale() === 'zh-CN' ? 'bg-gray-100 text-black' : '' %>">
                            简
                        </a>
                        <a href="/lang/zh-TW"
                            class="border border-gray-300 px-2 py-1 hover:bg-blue-50 hover:text-blue-600 hover:border-blue-400 transition <%= getLocale() === 'zh-TW' ? 'bg-gray-100 text-black' : '' %>">
                            繁
                        </a>
                        <a href="/lang/en"
                            class="border border-gray-300 px-2 py-1 hover:bg-blue-50 hover:text-blue-600 hover:border-blue-400 transition <%= getLocale() === 'en' ? 'bg-gray-100 text-black' : '' %>">
                            En
                        </a>
                    </div>

                </div>
            </div>
        </div>

        <div id="mobile-menu"
            class="hidden md:hidden bg-white border-t border-gray-100 absolute top-20 left-0 w-full shadow-xl z-40">
            <div class="px-4 pt-2 pb-4 space-y-1">
                <a href="/"
                    class="block px-3 py-3 rounded-md text-base font-medium text-gray-700 hover:text-blue-600 hover:bg-gray-50 transition">
                    <%= __('nav_home') %>
                </a>
                <a href="/about"
                    class="block px-3 py-3 rounded-md text-base font-medium text-gray-700 hover:text-blue-600 hover:bg-gray-50 transition">
                    <%= __('nav_about') %>
                </a>
                <a href="/contact"
                    class="block px-3 py-3 rounded-md text-base font-medium text-gray-700 hover:text-blue-600 hover:bg-gray-50 transition">
                    <%= __('nav_contact') %>
                </a>
            </div>
            <div class="border-t border-gray-100 px-7 py-4 bg-gray-50">
                <div class="flex items-center gap-4">
                    <span class="text-sm text-gray-500 font-medium">Language:</span>
                    <a href="/lang/zh-CN"
                        class="text-sm border border-gray-300 bg-white px-3 py-1 rounded shadow-sm hover:text-blue-600 <%= getLocale() === 'zh-CN' ? 'text-blue-600 border-blue-500 bg-blue-50' : '' %>">简</a>
                    <a href="/lang/zh-TW"
                        class="text-sm border border-gray-300 bg-white px-3 py-1 rounded shadow-sm hover:text-blue-600 <%= getLocale() === 'zh-TW' ? 'text-blue-600 border-blue-500 bg-blue-50' : '' %>">繁</a>
                    <a href="/lang/en"
                        class="text-sm border border-gray-300 bg-white px-3 py-1 rounded shadow-sm hover:text-blue-600 <%= getLocale() === 'en' ? 'text-blue-600 border-blue-500 bg-blue-50' : '' %>">En</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="relative bg-gray-900 text-white h-64">
        <div class="absolute inset-0">
            <img class="w-full h-full object-cover opacity-60" src="/images/home_banner.webp" alt="Banner">
        </div>
        <div class="relative max-w-7xl mx-auto h-full flex flex-col justify-center items-center text-center px-4">
            <h1 class="text-4xl font-extrabold text-white sm:text-5xl drop-shadow-lg tracking-wider">
                最新課程資訊
            </h1>
            <p class="mt-6 text-xl max-w-3xl mx-auto drop-shadow-md text-gray-200">
                屯門婦聯 - 探索我們的精彩課程
            </p>
        </div>
    </div>

    <main class="flex-grow">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
            <div class="flex justify-between items-end mb-8">
                <h2 class="text-3xl font-bold tracking-tight text-gray-900">
                    <%= __('section_hot') %>
                </h2>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-y-10 gap-x-6 xl:gap-x-8">

                <% if (courses.length===0) { %>
                    <p class="text-gray-500 col-span-4 text-center py-10">网站维护中，请稍后访问</p>
                    <% } %>

                        <% courses.forEach(function(item) { %>
                            <div
                                class="group relative bg-white border border-gray-100 rounded-lg overflow-hidden hover:shadow-xl transition-all duration-300">
                                <div
                                    class="w-full max-h-80 bg-gray-100 aspect-w-1 aspect-h-1 overflow-hidden group-hover:opacity-90">
                                    <img src="<%= item.image %>" loading="lazy" alt="<%= item.title %>"
                                        class="w-full h-full object-center object-contain p-4 group-hover:scale-105 transition-transform duration-300">
                                </div>

                                <div class="p-4 text-center">
                                    <h3 class="text-lg font-medium text-gray-900 mb-1">
                                        <a href="/course/<%= item._id %>">
                                            <span aria-hidden="true" class="absolute inset-0"></span>
                                            <%= item.title %>
                                        </a>
                                    </h3>
                                    <div class="mt-2 flex justify-center items-center gap-3">
                                        <p class="text-xl font-bold text-red-600">HK$<%= item.price %>
                                        </p>
                                    </div>
                                </div>
                            </div>
                            <% }); %>
            </div>
        </div>
        <% if (pages> 0) { %>
            <div class="mt-12 flex justify-center">
                <nav class="isolate inline-flex -space-x-px rounded-md shadow-sm" aria-label="Pagination">

                    <% if (current==1) { %>
                        <span
                            class="relative inline-flex items-center rounded-l-md px-2 py-2 text-gray-300 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0 cursor-not-allowed">
                            <span class="sr-only">Previous</span>
                            <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                                <path fill-rule="evenodd"
                                    d="M12.79 5.23a.75.75 0 01-.02 1.06L8.832 10l3.938 3.71a.75.75 0 11-1.04 1.08l-4.5-4.25a.75.75 0 010-1.08l4.5-4.25a.75.75 0 011.06.02z"
                                    clip-rule="evenodd" />
                            </svg>
                        </span>
                        <% } else { %>
                            <a href="/?page=<%= current - 1 %>"
                                class="relative inline-flex items-center rounded-l-md px-2 py-2 text-gray-400 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0">
                                <span class="sr-only">Previous</span>
                                <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                                    <path fill-rule="evenodd"
                                        d="M12.79 5.23a.75.75 0 01-.02 1.06L8.832 10l3.938 3.71a.75.75 0 11-1.04 1.08l-4.5-4.25a.75.75 0 010-1.08l4.5-4.25a.75.75 0 011.06.02z"
                                        clip-rule="evenodd" />
                                </svg>
                            </a>
                            <% } %>

                                <% var i=(Number(current)> 5 ? Number(current) - 4 : 1) %>
                                    <% if(i !==1) { %>
                                        <span
                                            class="relative inline-flex items-center px-4 py-2 text-sm font-semibold text-gray-700 ring-1 ring-inset ring-gray-300">...</span>
                                        <% } %>

                                            <% for(; i <=(Number(current) + 4) && i <=pages; i++) { %>
                                                <% if (i==current) { %>
                                                    <span aria-current="page"
                                                        class="relative z-10 inline-flex items-center bg-blue-600 px-4 py-2 text-sm font-semibold text-white focus:z-20 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600">
                                                        <%= i %>
                                                    </span>
                                                    <% } else { %>
                                                        <a href="/?page=<%= i %>"
                                                            class="relative inline-flex items-center px-4 py-2 text-sm font-semibold text-gray-900 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0">
                                                            <%= i %>
                                                        </a>
                                                        <% } %>
                                                            <% if (i==Number(current) + 4 && i < pages) { %>
                                                                <span
                                                                    class="relative inline-flex items-center px-4 py-2 text-sm font-semibold text-gray-700 ring-1 ring-inset ring-gray-300">...</span>
                                                                <% } %>
                                                                    <% } %>

                                                                        <% if (current==pages) { %>
                                                                            <span
                                                                                class="relative inline-flex items-center rounded-r-md px-2 py-2 text-gray-300 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0 cursor-not-allowed">
                                                                                <span class="sr-only">Next</span>
                                                                                <svg class="h-5 w-5" viewBox="0 0 20 20"
                                                                                    fill="currentColor"
                                                                                    aria-hidden="true">
                                                                                    <path fill-rule="evenodd"
                                                                                        d="M7.21 14.77a.75.75 0 01.02-1.06L11.168 10 7.23 6.29a.75.75 0 111.04-1.08l4.5 4.25a.75.75 0 010 1.08l-4.5 4.25a.75.75 0 01-1.06-.02z"
                                                                                        clip-rule="evenodd" />
                                                                                </svg>
                                                                            </span>
                                                                            <% } else { %>
                                                                                <a href="/?page=<%= parseInt(current) + 1 %>"
                                                                                    class="relative inline-flex items-center rounded-r-md px-2 py-2 text-gray-400 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0">
                                                                                    <span class="sr-only">Next</span>
                                                                                    <svg class="h-5 w-5"
                                                                                        viewBox="0 0 20 20"
                                                                                        fill="currentColor"
                                                                                        aria-hidden="true">
                                                                                        <path fill-rule="evenodd"
                                                                                            d="M7.21 14.77a.75.75 0 01.02-1.06L11.168 10 7.23 6.29a.75.75 0 111.04-1.08l4.5 4.25a.75.75 0 010 1.08l-4.5 4.25a.75.75 0 01-1.06-.02z"
                                                                                            clip-rule="evenodd" />
                                                                                    </svg>
                                                                                </a>
                                                                                <% } %>
                </nav>
            </div>
            <% } %>
                </div>
    </main>

    <footer class="bg-white border-t border-gray-200 mt-auto">
        <div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
            <p class="text-center text-base text-gray-400">
                &copy; <%= __('footer_rights') %>
            </p>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const searchInput = document.getElementById('search-input');
            const resultsContainer = document.getElementById('search-results');
            let debounceTimer;

            const menuBtn = document.getElementById('mobile-menu-btn');
            const mobileMenu = document.getElementById('mobile-menu');

            if (menuBtn && mobileMenu) {
                menuBtn.addEventListener('click', (e) => {
                    e.stopPropagation();
                    mobileMenu.classList.toggle('hidden');
                });

                mobileMenu.querySelectorAll('a').forEach(link => {
                    link.addEventListener('click', () => {
                        mobileMenu.classList.add('hidden');
                    });
                });

                document.addEventListener('click', (e) => {
                    if (!mobileMenu.contains(e.target) && !menuBtn.contains(e.target)) {
                        mobileMenu.classList.add('hidden');
                    }
                });
            }

            searchInput.addEventListener('input', (e) => {
                const query = e.target.value.trim();

                clearTimeout(debounceTimer);

                if (query.length === 0) {
                    resultsContainer.classList.add('hidden');
                    resultsContainer.innerHTML = '';
                    return;
                }

                debounceTimer = setTimeout(async () => {
                    try {
                        const res = await fetch(`/api/course/search?q=${encodeURIComponent(query)}`);
                        const json = await res.json();

                        if (json.status === '0000' && json.data.length > 0) {
                            const html = json.data.map(course => `
                            <a href="/course/${course._id}" class="block px-4 py-3 hover:bg-gray-50 border-b border-gray-50 flex items-center transition">
                                <div class="flex-shrink-0 h-10 w-10 bg-gray-200 rounded overflow-hidden">
                                    <img src="${course.image}" alt="${course.title}" class="h-full w-full object-cover" onerror="this.src='/images/placeholder.png'">
                                </div>
                                <div class="ml-3">
                                    <div class="text-sm font-medium text-gray-900">${course.title}</div>
                                    <div class="text-xs text-red-500">HK$${course.price}</div>
                                </div>
                            </a>
                        `).join('');

                            resultsContainer.innerHTML = html;
                            resultsContainer.classList.remove('hidden');
                        } else {
                            resultsContainer.innerHTML = '<div class="p-4 text-center text-gray-500 text-sm">未找到相关课程</div>';
                            resultsContainer.classList.remove('hidden');
                        }
                    } catch (err) {
                        console.error('搜索出错:', err);
                    }
                }, 300);
            });

            searchInput.addEventListener('focus', () => {
                if (searchInput.value.trim().length > 0 && resultsContainer.innerHTML !== '') {
                    resultsContainer.classList.remove('hidden');
                }
            });

            document.addEventListener('click', (e) => {
                if (!searchInput.contains(e.target) && !resultsContainer.contains(e.target)) {
                    resultsContainer.classList.add('hidden');
                }
            });
        });
    </script>
</body>

</html>